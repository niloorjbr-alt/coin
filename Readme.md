# 🪙 TalaBot
A simple Telegram bot that displays the latest gold and currency prices in Iran.

---

## 🌟 Features
- Get real-time prices for gold, USD, EUR, and coins  
- Easy-to-use inline keyboard  
- Fast and reliable

---

## ⚙️ Install and Run

1️⃣ **Install dependencies:**
```bash
pip install -r requirements.txt
```

2️⃣ **Set up your bot token:**  
Insert your *Telegram Bot Token* (from BotFather) into the `TOKEN` variable in the `bot.py` file:
```python
TOKEN = "YOUR_TELEGRAM_BOT_TOKEN_HERE"
```

---

## 🇮🇷 توضیحات فارسی

این ربات تلگرام برای نمایش قیمت لحظه‌ای طلا و ارز طراحی شده است.  

### 💡 امکانات:
- نمایش قیمت لحظه‌ای دلار، یورو، سکه و طلا  
- رابط کاربری ساده با کیبورد درون‌خطی  
- سرعت بالا و پاسخ‌گویی سریع  

---

## ⚙️ نصب و اجرا

۱️⃣ **نصب کتابخانه‌ها:**
```bash
pip install -r requirements.txt
```

۲️⃣ **تنظیم توکن ربات:**  
*توکن ربات تلگرام* خود (از BotFather) را در متغیر `TOKEN` در فایل `bot.py` وارد کنید:
```python
TOKEN = "YOUR_TELEGRAM_BOT_TOKEN_HERE"
```
